{
  "name": "video-haptic-joystick",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Extract simulated vibration (footsteps, engine, touch) from a video's audio & motion and drive a gamepad's rumble motors via the Web Gamepad API.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
